node_modules
dist
*.tsbuildinfo
*.local
.DS_Store

# The demo database itself IS committed (data/flipr.sqlite).
# Only ignore the transient WAL sidecar files.
data/*.sqlite-wal
data/*.sqlite-shm

# Local Claude Code tooling (not part of the app)
.claude
