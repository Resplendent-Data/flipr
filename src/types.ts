// Re-export the shared API contract so components import from one place.
export type {
  CurrentUser,
  Match,
  NarwhalProfile,
  SwipeDirection,
  SwipeRequest,
  SwipeResponse,
} from '../shared/types.ts'
